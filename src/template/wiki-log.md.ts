export const LOG_TEMPLATE = `# 变更日志

以追加方式记录 wiki 操作。

日志语言必须使用 \`.llm-wiki-invest/config.toml\` 中 \`vault.language\` 指定的语言；动作词可保留英文，例如 \`init\`、\`dossier-apply\`、\`ingest\`、\`query\`、\`sync\`。

格式：\`[YYYY-MM-DD] action | subject\`
`;
